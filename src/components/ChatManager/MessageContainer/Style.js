import { makeStyles } from '@material-ui/core/styles';

const Style = makeStyles({
    messageArea: {
      height: '70vh',
      width:'100%',
      overflowY: 'auto',
      padding:'3%'
    }
  });

  export default Style;